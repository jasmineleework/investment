# OS
.DS_Store

# IDE
.vscode/
.idea/

# History context (local reference only)
history_context/

# Design docs — local drafts, not for shared repo
doc/

# Claude — local instructions, not for shared repo
.claude/
.mcp.json
CLAUDE.md

# Research output (local only, not tracked)
Research/

# Node
node_modules/
package-lock.json

# Python
__pycache__/
*.pyc

# Morning Update — user-private watchlist (use watchlist.json.example as template)
investment-plugin/references/watchlist.json

# Personal task tracking and research run logs (local-only, not for shared repo)
tasks/
